# RealiMotion Demo (minimal)

替换 `assets/videos`、`assets/thumbs`、`assets/preview_gifs` 为你的媒体文件，然后 push 到 GitHub 并启用 Pages 即可。

## 本地预览
直接用浏览器打开 `index.html` 或：
```bash
python -m http.server 8000
# 然后访问 http://localhost:8000
